# CONFIG_ZMK_COMPANION_DISPLAY used to be defined here. It's now owned by the
# oscampo/zmk-companion west module (added in config/west.yml), which brings
# its own Kconfig - see that repo's zephyr/module.yml and firmware/Kconfig.
